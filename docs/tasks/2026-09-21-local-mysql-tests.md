# Local MySQL integration-test task packet

AGENTS_APPLIED=true. User instruction on 2026-09-21 explicitly replaces Docker/Testcontainers test policy with local MySQL 8; no H2, no test suppression or assertion weakening.

- Branch: chun9930. Existing three Part2 commits moved and pushed; previous integration branch deleted after ancestry check. Old divergent local chun9930 preserved under codex/chun9930-local-preserved-20260921.
- Allowlist: build.gradle; the seven named integration tests; new src/test/java/com/travelbird/support helpers and safety tests; test-only META-INF/spring.factories; .env.example test placeholders; README test instructions; harness status and daily log. Production/API/source/DBML/migrations remain unchanged.
- Scan: seven real container classes + three dependencies only. src/mysqlIntegrationTest/TripSchemaMySqlIntegrationTest is a pre-existing inactive source set with a stale support reference, not part of clean test; do not alter.
- Design: ContextCustomizerFactory for all @SpringBootTest (including contextLoads), class-specific cache key. Generate a fresh travelbird_test_UUID schema through server-only JDBC connection; never accept a database name or DB_URL. TEST_DB_HOST/PORT/USERNAME/PASSWORD only. MySQL8 validation; Flyway enabled, clean disabled, JPA validate. Existing transactional test methods roll back. Context disposal closes dependent DataSource before dropping owned schema; JVM hook fallback for context startup failure. Exact generated-name guard + successful creation ownership required before deletion. No fallback credentials, sanitized failures.
- Plan: safety regression tests RED -> implement common lifecycle -> replace seven container blocks unchanged assertions -> remove deps only after zero remaining uses -> compileJava/compileTestJava -> Trip/AI/auth -> all seven actual DB suites -> diagnose failures by exact cause -> clean test -> independent safety review -> commit/push chun9930 -> create/update PR20.
- Evidence: record development travelbird schema/data digest before/after via read-only JDBC; report equality without exposing data. Record generated schemas and cleanup; keep test output free of credentials.
- Team implementation: reused main(PR15 UserReaderImpl) + PR14 SavedRouteReferenceReaderImpl with original commits; no new Reader.
